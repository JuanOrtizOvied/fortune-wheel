{
  "name": "fortune-wheel",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite --host --port 5001",
    "build": "vite build",
    "preview": "vite preview",
    "bootstrap": "rm -rf node_modules && yarn install"
  },
  "devDependencies": {
    "vite": "^4.2.0"
  },
  "dependencies": {
    "sass": "^1.62.0"
  }
}
